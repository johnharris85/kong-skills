interface:
  display_name: "Web Search"
  short_description: "Search and read web sources with the Kong MCP endpoint."
dependencies:
  tools:
    - type: "mcp"
      value: "kong"
      description: "Kong remote MCP server for web search and page retrieval."
      transport: "streamable_http"
      url: "https://us.mcp.konghq.com"
